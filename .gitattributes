# Reduce CSS influence but keep it in language stats
style.css linguist-detectable=true

# Prioritize JavaScript as the dominant language
*.js linguist-detectable=true

# Still ignore unrelated/minified files (if added in future)
*.min.js linguist-detectable=false
